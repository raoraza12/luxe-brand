{
  "name": "luxe-brand",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client-install": "cd client && npm install",
    "server-install": "cd server && npm install",
    "install-all": "npm run client-install && npm run server-install",
    "build": "npm run install-all && cd server && npx prisma generate && cd .. && npm run build --prefix client"
  }
}
